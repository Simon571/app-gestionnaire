name: gestionnaire_app
description: Application mobile pour les utilisateurs de l'assemblée.
publish_to: 'none'

version: 1.0.2+102

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  riverpod: ^3.0.3
  flutter_riverpod: ^3.0.3
  riverpod_generator: ^3.0.3
  
  # Networking & Storage
  dio: ^5.3.1
  http: ^1.1.0
  shared_preferences: ^2.2.2
  path_provider: ^2.1.0
  path: ^1.8.3
  pdfx: ^2.9.2
  open_filex: ^4.3.2
  
  # UI & Navigation
  go_router: ^17.0.1
  flutter_native_splash: ^2.3.0
  
  # Utilities
  intl: ^0.20.2
  uuid: ^4.0.0
  crypto: ^3.0.3
  connectivity_plus: ^7.0.0
  
  # Date & Time
  timezone: ^0.10.1
  
  # Firebase (Auto-update) - Disabled for Windows due to CMake issues
  # firebase_core: ^2.24.2
  package_info_plus: ^8.0.0
  url_launcher: ^6.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.6
  # flutter_launcher_icons: ^0.14.4

# flutter_launcher_icons:
#   android: true
#   ios: false
#   image_path: "assets/app_icon.jpg"

flutter:
  uses-material-design: true
  
  assets:
    - assets/data/
