
import { Card, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export default function AlertesPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Voir les alertes</CardTitle>
        <CardDescription>Cette page affichera les alertes pour l'utilisateur.</CardDescription>
      </CardHeader>
    </Card>
  );
}
