
import { Card, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export default function MisesAJourPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Mises à jour</CardTitle>
        <CardDescription>Cette page affichera les informations sur les mises à jour.</CardDescription>
      </CardHeader>
    </Card>
  );
}
