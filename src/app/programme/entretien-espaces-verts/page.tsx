
'use client';

import { Card, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export default function EntretienEspacesVertsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Entretien des espaces verts</CardTitle>
        <CardDescription>Cette page affichera le programme d'entretien des espaces verts.</CardDescription>
      </CardHeader>
    </Card>
  );
}
