
'use client';

import { Card, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export default function TemoignagePublicPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Témoignage public</CardTitle>
        <CardDescription>Cette page affichera les informations sur le témoignage public.</CardDescription>
      </CardHeader>
    </Card>
  );
}
