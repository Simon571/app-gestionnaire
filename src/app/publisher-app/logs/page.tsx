
import { Card, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export default function LogsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Journaux</CardTitle>
        <CardDescription>Cette page affichera les journaux d'activité de l'application mobile.</CardDescription>
      </CardHeader>
    </Card>
  );
}
