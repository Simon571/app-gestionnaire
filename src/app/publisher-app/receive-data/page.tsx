
import { Card, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export default function ReceiveDataPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Recevoir les données</CardTitle>
        <CardDescription>Cette page servira à recevoir des données.</CardDescription>
      </CardHeader>
    </Card>
  );
}
