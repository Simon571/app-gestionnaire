
import { Card, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export default function SendDataPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Envoyer les données</CardTitle>
        <CardDescription>Cette page servira à envoyer des données.</CardDescription>
      </CardHeader>
    </Card>
  );
}
