
import { Card, CardHeader, CardTitle, CardDescription } from "@/components/ui/card";

export default function PreachingOverseerPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Responsable de la prédication</CardTitle>
        <CardDescription>Gérez les paramètres et les attributions pour cette responsabilité.</CardDescription>
      </CardHeader>
    </Card>
  );
}
